"""Work in progress -- not part of the stable, documented pipeline yet.

Modules here are still being validated (no real usage data has gone through
them) and their CLI/API can change or disappear without the usual care taken
for `zhongwen_anki`'s other modules. Once a module is considered done, move it
up to `zhongwen_anki/` proper (update its import path, the console-script
entry point in pyproject.toml, and its test's location).
"""
